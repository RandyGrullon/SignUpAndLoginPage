<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="SignUpAndLoginPage.Views.ContentViews.GeneralContentView"
             x:Name ="CVkey">
   
    
  <ContentView.Content>
        <StackLayout BindingContext="{x:Reference CVkey}">
           
            <Image Source="{Binding ImageView}" HeightRequest="100"  Style="{StaticResource ImagenGeneralContentView}"/>
            <Label Text="{Binding LabelTitle}" TextColor="Black" FontSize="20" HorizontalTextAlignment="Center"/>
            <Label Text="{Binding LabelSubTitle}" TextColor="Black" FontSize="15" HorizontalTextAlignment="Center"/>


        </StackLayout>
  </ContentView.Content>
</ContentView>
